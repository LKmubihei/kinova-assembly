hyy_controller:
  joints: {
    type: string_array,
    default_value: [],
    description: "Name of the joints to control",
  }
  state_interfaces: {
    type: string_array,
    default_value: [],
    description: "Name of the interfaces to state",
  }
  command_interfaces: {
    type: string_array,
    default_value: [],
    description: "Name of the interfaces to command",
  }
  component_index: {
    type: int,
    default_value: 0,  
    description: "Index of the component to command",
  }
  if_additionaxis: {
    type: int,
    default_value: 0,  
    description: "Type of the component",
  }
